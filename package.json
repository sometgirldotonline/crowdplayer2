{
  "dependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "daisyui": "^5.7.0",
    "tailwindcss": "^4.3.3"
  },
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i styles.not_dist.css -o styles.dist.css",
    "watch:css": "npx @tailwindcss/cli -i styles.not_dist.css -o styles.dist.css --watch"
  }
}
